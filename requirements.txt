Flask==3.0.3
gunicorn==20.1.0
Jinja2==3.1.4
Werkzeug==3.0.3
requests==2.32.3
numpy>=1.21,<1.27       # This version range is lightweight and compatible
pandas==2.1.0           # Pandas is still needed for data handling
scikit-learn==1.2.2     # Lightweight machine learning library
# tensorflow-cpu==2.17.0  # Commented out; use only if absolutely necessary
# torch==2.2.2            # Commented out; use only if absolutely necessary
transformers==4.44.0    # Needed for NLP tasks, but consider using smaller models
spacy==3.7.5            # Core NLP library
nltk==3.8.1             # For NLP-related tasks, can be removed if not crucial
matplotlib==3.8.0       # For any plotting needs, can be removed if not crucial
seaborn==0.12.2         # Built on matplotlib, can be removed if not crucial
pyttsx3==2.90
torch==1.10.0
